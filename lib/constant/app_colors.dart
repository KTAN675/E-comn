import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  // ===== Core Brand =====
  static const primaryOrange = Color(0xFFFF660E);
  static const secondaryCyan = Color(0xFF00C3D0);
  static const dangerRed = Color(0xFFFF0F0F);

  // ===== Neutrals =====
  static const background = Color(0xFFF4F4F4);
  static const white = Colors.white;
  static const black = Color(0xFF1E1E1E);
  static const greyText = Color(0xFF626262);
  static const border = Color(0xFFF5F5F5);

  // ===== Dynamic Accent (default) =====
  static Color accent = primaryOrange;
}